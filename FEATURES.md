# ✨ Функциональные возможности AI TestOps Copilot

AI TestOps Copilot — это интеллектуальная система для автоматизации тестирования, использующая современные LLM для генерации, валидации и выполнения тестов.

## 🤖 Основные возможности

### 1. Генерация UI/E2E тестов
Автоматическое создание тестов на основе анализа веб-страниц.

*   **Входные данные:** URL страницы или HTML контент.
*   **Анализ:** Система скачивает реальный контент страницы, анализирует структуру DOM, находит интерактивные элементы (кнопки, поля ввода, ссылки).
*   **Мульти-модельная генерация:** Использует 3 параллельные нейросети для создания наиболее надежных сценариев.
*   **Технологии:** Python + Selenium + Pytest.
*   **Результат:** Готовый к запуску код теста с Page Object паттернами (опционально) и проверками.

### 2. Генерация ручных тест-кейсов
Преобразование текстовых требований в структурированные тест-кейсы.

*   **Входные данные:** Текстовое описание требований, User Stories.
*   **Результат:** Список тест-кейсов с шагами, ожидаемыми результатами и приоритетами.
*   **Формат:** JSON, Markdown.

### 3. Генерация API тестов
Создание тестов для REST API на основе спецификации.

*   **Входные данные:** OpenAPI (Swagger) спецификация (JSON/YAML).
*   **Результат:** Pytest тесты, покрывающие основные эндпоинты, проверки статус-кодов и схем ответов.
*   **Покрытие:** Расчет покрытия эндпоинтов тестами.

### 4. Анализ покрытия кода (Code Coverage)
Интеллектуальный анализ покрытия кода тестами и генерация недостающих тестов.

*   **Анализ:** Загрузка исходного кода проекта для расчета метрик покрытия (Lines, Functions, Branches).
*   **Визуализация:** Детальный отчет о покрытии файлов и функций.
*   **Smart Generation:** Автоматическое выявление непокрытых функций и генерация тестов специально для них.
*   **Приоритезация:** Определение приоритета покрытия для критически важных участков кода.

### 5. Интеграция с Allure Report
Автоматическое добавление отчетности в генерируемые тесты.

*   **Декораторы:** `@allure.step`, `@allure.feature`, `@allure.story`.
*   **Severity:** Автоматическое проставление уровней критичности (`severity_level.CRITICAL`, `severity_level.NORMAL`).
*   **Метаданные:** Тегирование тестов для удобной фильтрации.

### 6. Валидация и Самокоррекция (Self-Healing)
Система не просто генерирует код, но и проверяет его работоспособность.

*   **Синтаксическая проверка:** Валидация Python кода перед отдачей пользователю.
*   **Агрегация:** Объединение лучших частей кода от разных моделей для устранения ошибок.
*   **Исправление ошибок:** Если тест падает при запуске, система может проанализировать лог ошибки и предложить исправление (в разработке).


## 🛠️ Технический стек

*   **Backend:** FastAPI, Python 3.10
*   **AI/LLM:** Cloud.ru Evolution API (Qwen, GPT-OSS, T-pro)
*   **Database:** PostgreSQL (хранение истории, пользователей)
*   **Cache:** Redis (кэширование запросов, rate limiting)
*   **Frontend:** React, TypeScript, Vite, Tailwind CSS
*   **Testing:** Pytest, Selenium, Allure
