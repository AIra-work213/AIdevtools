# 🚀 Запуск проекта локально

Этот документ описывает пошаговый процесс запуска проекта AI TestOps Copilot на локальной машине.

## 📋 Предварительные требования

Перед началом убедитесь, что у вас установлены:

1.  **Docker** и **Docker Compose** (версия 3.8+)
2.  **Git**
3.  **Python 3.10+** (опционально, для локальной разработки без Docker)
4.  **Node.js 18+** (опционально, для локальной разработки фронтенда)

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/AIra-work213/AIdevtools
cd AIdevtools
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example` (если есть) или используйте следующие значения:

```bash
# Database
POSTGRES_USER=testops_user
POSTGRES_PASSWORD=testops_password
POSTGRES_DB=testops_db

# AI Service (Cloud.ru Evolution)
CLOUD_API_KEY=your_api_key_here
CLOUD_API_URL=https://foundation-models.api.cloud.ru/v1
CLOUD_MODEL=openai/gpt-oss-120b

# Security
SECRET_KEY=your_secret_key_here
ENVIRONMENT=development
```

> **Важно:** Для работы генерации тестов вам понадобится валидный `CLOUD_API_KEY`.

## 🐳 Запуск через Docker Compose (Рекомендуемый способ)

Самый простой способ запустить все сервисы (Backend, Frontend, Database, Redis).

```bash
docker-compose up --build
```

После успешного запуска сервисы будут доступны по адресам:

*   **Frontend:** [http://localhost:3001](http://localhost:3001)
*   **Backend API:** [http://localhost:8001](http://localhost:8001)
*   **API Documentation:** [http://localhost:8001/docs](http://localhost:8001/docs)

### Полезные команды Docker

*   **Остановить сервисы:** `docker-compose down`
*   **Перезапустить только backend:** `docker-compose restart backend`
*   **Просмотр логов:** `docker-compose logs -f backend`

## 💻 Локальный запуск (для разработки)

Если вы хотите запускать сервисы без Docker (например, для отладки).

### Backend

1.  Перейдите в директорию бэкенда:
    ```bash
    cd src/backend
    ```
2.  Создайте и активируйте виртуальное окружение:
    ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/macOS
    # venv\Scripts\activate  # Windows
    ```
3.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
4.  Установите Playwright (для UI тестов):
    ```bash
    playwright install chromium
    ```
5.  Запустите сервер:
    ```bash
    uvicorn app.main:app --reload --port 8001
    ```

### Frontend

1.  Перейдите в директорию фронтенда:
    ```bash
    cd src/frontend
    ```
2.  Установите зависимости:
    ```bash
    npm install
    ```
3.  Запустите сервер разработки:
    ```bash
    npm run dev
    ```
    Приложение будет доступно на [http://localhost:3001](http://localhost:3001) (порт может отличаться, см. консоль).

## 🧪 Запуск тестов

### Unit тесты (Backend)

```bash
# Из контейнера backend
docker-compose exec backend pytest

# Или локально
pytest
```

### UI Тесты (Генерация и запуск)

Проект поддерживает генерацию и запуск UI тестов прямо в Docker контейнере.
Для проверки конфигурации ChromeDriver в Docker:

```bash
docker-compose exec backend python3 test_chromedriver.py
```
