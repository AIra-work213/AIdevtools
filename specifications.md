# Project Specifications
Техническое задание на разработку системы "TestOps Copilot"
Версия документа: 1.0
Дата: 06.12.2025
Статус: Утверждено к разработке

1. Общие положения
1.1. Цель проекта
Разработка интеллектуального ассистента (Agentic AI System) на базе Cloud.ru Evolution Foundation Model, предназначенного для автоматизации рутинных операций QA-инженеров. Система должна сократить время на написание, рефакторинг и поддержку тестовой документации и автотестов, используя подход TestOps as Code.

1.2. Целевая аудитория
Manual QA Engineers (генерация кода ручных тестов).

Automation QA Engineers (генерация бойлерплейта автотестов).

QA Leads (валидация стандартов, ревью кода, поиск дубликатов).

1.3. Глоссарий
TestOps as Code: Подход, при котором тест-кейсы хранятся в репозитории кода (Python-файлы) и синхронизируются с TMS (Allure TestOps).

AAA (Arrange-Act-Assert): Паттерн написания тестов (Подготовка -> Действие -> Проверка).

LLM: Large Language Model (здесь: Cloud.ru Evolution).

Agent: Программный модуль, способный планировать действия и вызывать внешние инструменты.

2. Архитектура системы
2.1. Компонентная схема
Система состоит из четырех основных слоев:

Frontend (Client Layer):

SPA на React + TypeScript.

Интерфейс: Чат с ассистентом + IDE-подобный редактор кода (Monaco Editor).

Backend (API Layer):

REST API на Python (FastAPI).

Оркестратор запросов (LangChain / LangGraph).

AI Core (Intelligence Layer):

Интеграция с Cloud.ru Evolution API (Completion & Chat endpoints).

RAG-модуль (Retrieval Augmented Generation) для работы с контекстом проекта.

Векторное хранилище (ChromaDB/FAISS) для поиска дубликатов.

Integrations (External Layer):

GitLab API: Чтение репозиториев, создание Merge Requests.

Allure TestOps: (Опционально в MVP) синхронизация статусов.

OpenAPI/Swagger: Парсинг спецификаций для генерации тестов.

2.2. Стек технологий
Компонент	Технология	Версия	Примечание
Frontend	React + TypeScript	18/19	Vite сборщик, TailwindUI
State Mgmt	Zustand / React Query	Latest	Управление состоянием чата
Backend	Python + FastAPI	3.10+	Асинхронная архитектура
AI Framework	LangChain	Latest	Оркестрация агентов
LLM Provider	Cloud.ru Evolution	-	Модель поддерживающая русский язык
Vector DB	ChromaDB	Latest	Локальное или in-memory хранение
CI/CD	GitLab CI	-	Пайплайны сборки и деплоя
Container	Docker	Latest	Docker Compose для локального запуска
3. Функциональные требования
3.1. Модуль 1: Генерация ручных тестов (Manual Code Generation)
Задача: Преобразование неструктурированных требований в Python-код формата Allure.

Входные данные:

Текст требований (User Story).

Изображение (скриншот интерфейса) — в будущих версиях (Multimodal).

Параметры мета-данных (Feature, Story, Owner).

Логика обработки:

Анализ текста на наличие критериев приемки (Acceptance Criteria).

Декомпозиция на атомарные сценарии.

Генерация кода с применением паттерна AAA.

Добавление декораторов @allure.manual, @allure.step.

Выходные данные: Валидный Python-файл.

3.2. Модуль 2: Генерация автотестов (Automation Code Generation)
Задача: Создание исполняемого кода тестов для pytest.

3.2.1. UI Tests (E2E)
Контекст: Пользователь предоставляет HTML-сниппет или описание селекторов.

Стек: pytest + playwright (или selenium).

Требование: Генерация PageObject моделей (опционально) или прямых вызовов драйвера внутри теста.

3.2.2. API Tests
Вход: Файл спецификации OpenAPI (json/yaml) или URL на Swagger UI.

Логика:

Парсинг эндпоинтов, методов, параметров и схем ответов.

Генерация позитивных тестов (Happy Path).

Генерация негативных тестов (валидация типов данных, отсутствующие поля).

Требование: Использование библиотеки requests или httpx. Валидация JSON Schema ответа.

3.3. Модуль 3: Оптимизация и Валидация (Test Optimizer)
Задача: Повышение качества существующей тестовой базы.

Функция "Поиск дубликатов":

Использовать эмбеддинги (векторные представления) названий и шагов тестов.

Выявлять семантически похожие тесты (similarity > 0.85).

Функция "Линтер стандартов":

Проверка наличия обязательных декораторов (@allure.feature, @allure.owner).

Проверка структуры шагов (наличие with allure.step(...)).

Проверка наличия assert в конце теста.

4. Требования к интерфейсам (API & Data)
4.1. Формат выходного кода (Строгий стандарт)
Система должна генерировать код, строго соответствующий следующему шаблону:

python
import allure
import pytest
from allure_commons.types import AttachmentType

@allure.feature("{Feature Name}")
@allure.story("{Story Name}")
@allure.label("owner", "{User}")
@allure.tag("generated_by_ai")
class Test{ClassName}:

    @allure.title("{Human Readable Test Title}")
    @allure.severity(allure.severity_level.{LEVEL})
    @allure.manual  # Если тест ручной
    def test_{method_name}(self):
        """
        {Description if needed}
        """
        with allure.step("Arrange: {Подготовка данных}"):
            # Код подготовки
            pass

        with allure.step("Act: {Действие пользователя}"):
            # Код действия
            pass

        with allure.step("Assert: {Ожидаемый результат}"):
            # Проверки
            pass
4.2. API Эндпоинты (Draft)
Метод	URL	Описание	Body (Input)
POST	/api/v1/generate/manual	Генерация ручных тестов	{ "requirements": "text", "meta": {...} }
POST	/api/v1/generate/auto/api	Генерация API тестов	{ "openapi_spec": "json_string", "endpoint": "/vms" }
POST	/api/v1/analyze/validate	Проверка кода на стандарты	{ "code_snippet": "python_code" }
POST	/api/v1/gitlab/mr	Создание MR с тестами	{ "project_id": 123, "code": "...", "branch": "..." }
5. План реализации и Роли в команде
5.1. Команда и Зоны ответственности
Product Manager (PM):

Сбор требований от внутренних QA команд.

Подготовка датасетов для демо (Price Calculator, Evolution API).

Приемка качества генерации (User Acceptance Testing).

Data Scientist (DS):

Разработка системных промптов (System Prompts) для Cloud.ru Evolution.

Настройка RAG (индексация документации Allure и внутренних гайдлайнов).

Настройка параметров генерации (temperature, top_p) для минимизации галлюцинаций кода.

Backend / NLP Engineer:

Разработка FastAPI сервиса.

Интеграция с GitLab API и Cloud.ru API.

Реализация парсеров (OpenAPI -> Prompt context).

Frontend Developer:

Верстка чат-интерфейса и редактора кода (Monaco).

Визуализация диффов (сравнение версий кода).

Интеграция с бэкендом.

5.2. Этапы (Roadmap)
Этап 1: MVP Core (Недели 1-2)
Цель: Работающий генератор ручных тестов из текста.

Задачи:

[Backend] Базовый API + интеграция с LLM.

[DS] Промпт для генерации валидного Python (AAA pattern).

[Frontend] Простая форма ввода и вывода кода.

Этап 2: Advanced & Integrations (Недели 3-4)
Цель: Генерация API-тестов и интеграция с GitLab.

Задачи:

[Backend] Парсер OpenAPI. Интеграция с GitLab (создание коммитов).

[DS] Few-shot learning для API тестов (примеры хороших тестов в промпт).

[Frontend] Редактор кода с подсветкой синтаксиса.

Этап 3: Optimization & Polish (Недели 5-6)
Цель: Валидация, поиск дублей, финальный UI.

Задачи:

[Backend] Векторный поиск (ChromaDB).

[QA/PM] Тестирование на демо-кейсах (Калькулятор, VMs API).

Документация и Docker-образы.

6. Демонстрационные сценарии (Use Cases)
Для сдачи проекта необходимо продемонстрировать работу на двух кейсах:

Кейс 1: UI Testing (Price Calculator)
Вход: Текстовое описание функционала конфигуратора ВМ (зависимость цены от CPU/RAM).

Ожидаемый результат: Агент генерирует файл test_calculator.py с 15+ кейсами, покрывающими граничные значения (min/max RAM), проверку дефолтных значений и расчет цены. Все тесты помечены @allure.manual.

Кейс 2: API Testing (Evolution Compute)
Вход: Ссылка на Swagger/OpenAPI спецификацию сервиса Compute. Запрос: "Покрыть тестами эндпоинты управления дисками".

Ожидаемый результат: Агент генерирует test_disks_api.py, где реализованы проверки:

Создание диска (POST 200).

Получение списка (GET 200).

Попытка создания с невалидным размером (POST 400).

Удаление диска (DELETE 204).

7. Нефункциональные требования
Производительность: Время генерации одного файла с тестами (до 500 строк) не должно превышать 45 секунд.

Надежность: При ошибке API Cloud.ru система должна совершать повторную попытку (Retry policy) до 3 раз.

Безопасность:

Токены доступа (GitLab PAT, Evolution API Key) не должны передаваться на фронтенд.

Токены хранятся в .env файлах на бэкенде.

Качество кода:

Весь код бэкенда должен быть типизирован (Python Type Hints).

Покрытие юнит-тестами ядра системы > 60%.

8. Критерии приемки MVP
 Система развернута в Docker-контейнерах.

 Интерфейс позволяет ввести текст требований и получить Python-код.

 Сгенерированный код валиден (запускается интерпретатором без SyntaxError).

 Реализована структура кода Allure (декораторы, шаги).

 Продемонстрирована генерация тестов для Калькулятора и API Compute.

 Работает экспорт в файл (скачивание .py).

# 9. Примеры входов и выходов
Входы и выходы TestOps Copilot
TestOps Copilot принимает натуральный текст или файлы от QA-инженера и выдает готовый Python-код для TestOps. Вот точные форматы для каждого модуля.

Генерация ручных тест-кейсов (Manual Tests)
Вход:

text
"На странице калькулятора цен нужно проверить:
1. При выборе CPU=4 цена увеличивается
2. При добавлении диска 100GB цена пересчитывается
3. Кнопка 'Скачать PDF' работает"
Или файл: Скриншот UI + текст требований

Выход:

python
@allure.manual
@allure.feature("Price Calculator")
@allure.story("Compute Configuration")
class TestPriceCalculator:
    @allure.title("Проверка пересчета цены при CPU=4")
    def test_cpu_price_calculation(self):
        with allure.step("1. Открыть калькулятор цен"):
            pass
        with allure.step("2. Выбрать CPU=4"):
            pass
        with allure.step("3. Проверить пересчет цены"):
            pass
Генерация автотестов UI (E2E)
Вход:

text
URL: https://cloud.ru/price-calculator
Сценарий: Выбрать Compute → CPU=4 → Добавить диск 100GB → Проверить итоговую цену
HTML структура: <div class="cpu-selector">...</div>
Выход:

python
import pytest
from playwright.sync_api import Page

def test_price_calculation_e2e(page: Page):
    page.goto("https://cloud.ru/price-calculator")
    page.select_option(".cpu-selector", "4")
    page.click(".add-disk-button")
    page.fill(".disk-size", "100")
    assert "цена пересчитана" in page.locator(".total-price").text()
Генерация API тестов
Вход:

text
OpenAPI файл: evolution-compute-api.yaml
Эндпоинт: /vms
Нужно: CRUD операции + негативные сценарии
Или текст:

text
"Протестировать POST /vms с валидными данными, невалидными данными и без токена"
Выход:

python
@pytest.mark.api
@allure.feature("Evolution Compute")
class TestVmsApi:
    def test_create_vm_success(self):
        response = requests.post("/vms", json={"name": "test-vm", "flavor": "small"})
        assert response.status_code == 201
        assert "id" in response.json()
Валидация и оптимизация
Вход:

text
# Существующий код тест-кейсов из GitLab
@allure.feature("Login")
def test_login():  # ❌ Нет story, severity, шагов AAA
    pass
Выход:

text
✅ Проблемы найдены:
1. Отсутствует @allure.story
2. Нет шагов AAA (Arrange-Act-Assert)
3. Нет severity level

🔧 Исправленный код:
@allure.story("User Authentication")
@allure.severity(allure.severity_level.CRITICAL)
def test_login():
    with allure.step("Arrange: Открыть форму логина"):
        pass
    with allure.step("Act: Ввести логин/пароль"):
        pass
    with allure.step("Assert: Проверка успешного входа"):
        pass
Итоговый workflow пользователя
text
1. QA пишет: "Сделай тесты для калькулятора цен" [текст/скриншот]
2. Copilot → Генерирует 20 тест-кейсов [Python код]
3. QA → Нажимает "Проверить" → Получает отчет валидации
4. QA → Редактирует (опционально) → "Коммит в GitLab"
5. Copilot → Создает MR с тестами
Ключ: На входе — человеческий язык + файлы, на выходе — исполняемый Python код с Allure декораторами, готовый к запуску в CI/CD.



Пример подключение к cloud.ru API:
import os

from openai import OpenAI

api_key = os.environ["API_KEY"]
url = "https://foundation-models.api.cloud.ru/v1"

client = OpenAI(
    api_key=api_key,
    base_url=url
)

response = client.chat.completions.create(
    model="Qwen/Qwen3-Coder-480B-A35B-Instruct",
    max_tokens=2500,
    temperature=0.5,
    presence_penalty=0,
    top_p=0.95,
    messages=[
        {
            "role": "user",
            "content":"Как написать хороший код?"
        }
    ]
)

print(response.choices[0].message.content)