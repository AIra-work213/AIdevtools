# Quick Start Guide - New Features

## 🎯 Новые возможности

### 1️⃣ Многострочный ввод в чате

**Как использовать**:
- **Enter** - отправить сообщение
- **Shift+Enter** - перейти на новую строку

**Пример**:
```
Создай тесты для функции логина с проверкой:
1. Правильные credentials
2. Неправильный пароль
3. Пустые поля
4. SQL injection
[Shift+Enter для продолжения ввода]
[Enter для отправки]
```

### 2️⃣ Двухэтапная генерация с Allure

**Автоматически**:
- Stage 1: Генерация тестов на выбранном framework (pytest/unittest)
- Stage 2: Добавление Allure декораторов

**Результат**:
```python
import pytest
import allure
from allure_commons.types import Severity

@allure.feature("User Authentication")
@allure.story("Login Tests")
class TestLogin:
    
    @allure.title("Test successful login")
    @allure.severity(Severity.NORMAL)
    def test_login_success(self):
        with allure.step("Arrange: Setup credentials"):
            # ...
        with allure.step("Act: Perform login"):
            # ...
        with allure.step("Assert: Verify success"):
            # ...
```

### 3️⃣ Allure декораторы

Сгенерированные тесты включают Allure декораторы для:
- 📊 Организации тестов по feature и story
- 📝 Понятных названий тестов
- ⏱️ Отслеживания времени выполнения
- 📋 Структурирования шагов теста


## 🚀 Запуск

### Backend
```bash
cd src/backend
source venv/bin/activate
pip install -r requirements.txt  # Установит allure-pytest
python -m uvicorn app.main:app --reload
```

### Frontend
```bash
cd src/frontend
npm install
npm run dev
```

**Доступ**:
- Backend API: http://localhost:8000
- Frontend: http://localhost:3001
- API Docs: http://localhost:8000/api/v1/docs

## 🧪 Быстрый тест

### Тест генерации с Allure

1. Откройте Chat (http://localhost:3001)

2. Введите многострочный запрос (используя Shift+Enter):
```
Создай тесты для калькулятора
Функции:
- Сложение двух чисел
- Вычитание
- Умножение
```

3. Нажмите Enter - код сгенерируется с двумя этапами

4. Нажмите "▶️ Запустить" - увидите Allure отчет

### Ожидаемый результат
```
✅ Генерация завершена (Stage 1 + Stage 2)
✅ Код содержит pytest и Allure
✅ Валидация пройдена
✅ При запуске - Allure отчет:
   - Total: 3
   - Passed: 3
   - Failed: 0
```

## 📚 Документация

- **Полное описание**: `CHANGELOG_20251210.md`
- **Тесты**: `test_all_features.py`
- **API Docs**: http://localhost:8000/api/v1/docs

## ❓ FAQ

**Q: Как работает двухэтапная генерация?**
A: Первая модель создает логику тестов, вторая добавляет Allure аннотации. Это обеспечивает чистоту кода и правильную структуру.

**Q: Где хранятся Allure отчеты?**
A: В `/tmp/allure-results/{run_id}/` - каждый запуск создает уникальную папку.

**Q: Что делать если валидация провалилась?**
A: Система автоматически попытается исправить код через AI (до 2 раз). Если не получилось - показывает ошибки.

**Q: Можно ли отключить Allure?**
A: Да, в настройках генерации. Или просто не используйте декораторы - система автоопределяет Allure в коде.

## 🐛 Known Issues

1. **UI Test Generation** - endpoint требует доработки (low priority)
2. **Validation/Duplicates endpoints** - роутеры не подключены (low priority)
3. **Manual tests с @allure.manual** - не выполняются pytest (by design)

## 💡 Tips

1. **Используйте Shift+Enter** для сложных запросов с примерами
2. **Включайте AAA pattern** в настройках для структурированных тестов
3. **Указывайте framework явно** в настройках (pytest/unittest)
4. **Проверяйте Allure отчет** после каждой генерации для качества
